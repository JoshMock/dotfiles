{{- if .backup.enabled }}
[Unit]
Description=Scheduled backups, powered by autorestic.

[Service]
Type=oneshot
ExecStart=bash -c "autorestic --ci backup -a -- unlock && autorestic --ci forget --prune -a -- unlock"

[Install]
WantedBy=backup.timer
{{ end }}
